#tolua++
从原有的c++代码导出接口给Lua使用，用该工具方便快捷。但是这个开源工具一直没有更新，而且有一些bug。
因为项目中会使用到此工具，所以fork一个自己维护。

目前修改了对cpp string(二进制)的支持。
待修复的问题：
    char sz[1024]； 这种类型，在类型检查时限定了必须传入一个lua table。应修改为可传入string。

期待使用该工具的人一起维护。
        

